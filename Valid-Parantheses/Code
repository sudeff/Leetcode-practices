
class Solution:
    def isValid(self, s: str):
        n=len(s)
        for i in range(0,n):
            if s[i]==s[n-i-1]:
                return True
            
solution=Solution()
print(solution.isValid("()[]{}"))