from typing import List

class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        for i in nums:
            for j in nums:
                if i==j:
                    break
                else:
                    return (i)
            
solution=Solution()
print(solution.singleNumber([2,2,1]))