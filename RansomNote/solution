class Solution(object):
    def canConstruct(self, ransomNote, magazine):
        if set(ransomNote) == set (magazine):
            return True
        else:
            return False
        

solution = Solution()
print(solution.canConstruct("hello","elloh"))














##I 